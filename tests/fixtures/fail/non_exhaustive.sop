type Colour enum {
    Red
    Yellow
    Green
}

func test() {
    colour := Colour.Red

    var message string
    match colour {
    case Colour.Red:
        message = "Stop"
    case Colour.Yellow:
        message = "Wait"
    }
}
