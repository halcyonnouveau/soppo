package main

import "fmt"

type User struct {
	name string
}

func findUser(id int) ?*User {
	return nil
}

func main() {
	user := findUser(1)

	// Bug: trying to access in the wrong branch
	if user == nil {
		fmt.Println(user.name) // Error: user is nil here!
	}
}
