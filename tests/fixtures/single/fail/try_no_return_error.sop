package main

import "errors"

func parsePort(s string) (int, error) {
    return 8080, nil
}

// ERROR: function doesn't return error, can't use ?
func noErrorReturn() int {
    port := parsePort("8080") ?
    return port
}

func main() {
    noErrorReturn()
}
