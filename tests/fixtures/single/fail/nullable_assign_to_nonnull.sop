package main

type User struct {
    name string
}

func findUser(id int) ?*User {
    return nil
}

func printUser(user *User) {
    // requires non-nullable
}

func main() {
    maybeUser := findUser(1)  // ?*User (nullable)

    // ERROR: Cannot pass nullable to non-nullable parameter
    printUser(maybeUser)
}
